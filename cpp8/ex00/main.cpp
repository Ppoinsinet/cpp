#include <vector>
#include <list>
#include "easyfind.hpp"

int main()
{
    std::vector<int> tab;

    tab.push_back(2);
    tab.push_back(8);
    tab.push_back(3);
    tab.push_back(4);

    //::easyfind(tab, 9);
    return 0;
}